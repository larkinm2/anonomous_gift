<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <h1>ALL USERS</h1>
</head>
  <body>
  <% @users.each do |user| %>
    <%= link_to(user.first_name, user_path(user)) %>
    <%= link_to(user.last_name, user_path(user)) %>
      </br>
  <% end %>
  </body>
</html>
